run:
  output_dir: runs/smoke_001/wiki_clean
  seed: 42

wikipag:
  service_url: http://127.0.0.1:8897
  top_k_per_query: 8
  retrieval_timeout_s: 120

construction:
  target_active_concepts: 20
  max_topic_anchors: 12
  max_concept_queries: 24
  max_passages: 32
  max_extraction_passages: 10
  max_grounding_candidates: 40
  grounding_top_k: 4
  evidence_top_k: 6
  max_evidence_items_per_slot: 2
  max_card_concepts: 10

model:
  name: gpt-5.4
  max_completion_tokens: 4096
  concurrency: 4
  max_retries: 2
  timeout_s: 120
