Paper prompt files are intentionally absent.

`references/ca_mem.pdf` has not been recovered from the current accessible workspace, so the verbatim CA-Mem paper prompts cannot be extracted yet. Do not place reconstructed prompts in this directory.

Reconstructed build prompts live in `prompts/ca_mem/reconstructed/` and are labeled as transparent reconstructions.
